---
import { ArrowRight, ChevronDown } from "@lucide/astro";
---

<section
  class="relative pt-48 pb-72 w-full flex items-center justify-center overflow-hidden bg-background"
>
  <!-- Imagen de fondo difuminada con máscara -->
  <div
    class="absolute inset-0 z-0 bg-[url(/miranti/pattern.webp)] bg-cover mask-b-from-70% mask-b-to-100%"
  >
    <div class="absolute inset-0 bg-primary/40 backdrop-blur-sm"></div>
  </div>

  <!-- Contenido principal -->
  <div class="relative z-10 w-full max-w-5xl px-5 md:px-8 mx-auto text-center">
    <!-- Título principal - texto grande sin transparencia -->
    <h1
      class="text-4xl md:text-6xl lg:text-7xl text-gray-100 mb-12 md:mb-16 font-serif"
    >
      <span class="text-secondary"> Conecta </span>
      tu arte con el Perú,
      <span class="text-secondary"> transforma</span>
      tu pasión en oportunidades y
      <span class="text-secondary"> lleva </span>
      tus creaciones a un mercado nacional.
    </h1>

    <!-- Botón principal -->
    <a
      href="#cta"
      class="group inline-flex items-center justify-center gap-3 bg-secondary hover:bg-secondary/90 text-primary font-bold py-4 px-10 rounded-full transition-all duration-300 shadow-lg hover:shadow-2xl hover:scale-105 border border-white/30 text-xl"
    >
      Comienza gratis
      <ArrowRight
        size={22}
        stroke-width={2.5}
        class="group-hover:translate-x-1 transition-transform duration-300"
      />
    </a>

    <!-- Indicador sutil de scroll -->
    <div class="mt-16 md:mt-20 flex justify-center cursor-pointer">
      <div
        class="bg-black/70 backdrop-blur-md rounded-full border border-white/20 shadow-lg ring-1 ring-white/30 flex items-center justify-between py-2 px-2 md:py-4 md:px-4 animate-bounce"
      >
        <ChevronDown size={22} stroke-width={2} class="text-white" />
      </div>
    </div>
  </div>
</section>
