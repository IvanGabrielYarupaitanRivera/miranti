---
import { UserPlus, FileImage, Share2, ArrowRight } from "@lucide/astro";

const stepsData = [
  {
    image: " ",
    icon: UserPlus,
    title: "Regístrate",
    content: "Crea tu cuenta gratis en minutos con solo tu nombre, correo electrónico y región. Miranti está diseñado para ser accesible incluso con conexiones limitadas, permitiéndote completar el registro rápidamente desde cualquier dispositivo móvil sin complicaciones técnicas.",
  },
  {
    image: " ",
    icon: FileImage,
    title: "Crea tu catálogo",
    content: 
      "Personaliza tu espacio digital subiendo fotos de tus creaciones directamente desde tu celular. Añade descripciones, precios, disponibilidad y detalles culturales que hacen único tu trabajo. Nuestro sistema optimiza automáticamente las imágenes para cargar rápidamente incluso con internet lento.",
  },
  {
    image: " ",
    icon: Share2,
    title: "Comparte y vende",
    content:
      "Genera un enlace personalizado a tu catálogo que puedes compartir fácilmente por WhatsApp, redes sociales o correo electrónico. Los clientes pueden explorar tus productos, hacer consultas directas y realizar pedidos sin intermediarios. Recibe notificaciones instantáneas y gestiona tus ventas desde un solo lugar.",
  },
];
---

<section
  id="comofunciona"
  class="py-16 md:py-24 lg:py-32 relative overflow-hidden"
>
  <!-- Degradado elegante con tres capas usando Tailwind 4 -->
  <div class="absolute inset-0" aria-hidden="true">
    <!-- Degradado principal de fondo -->
    <div
      class="absolute inset-0 bg-linear-to-b from-black via-primary to-black"
    >
    </div>

    <!-- Degradado con color secundario para detalle y profundidad -->
    <div
      class="absolute inset-0 bg-linear-to-tl from-tertiary/50 via-black/50 to-secondary/50"
    >
    </div>

    <!-- Degradado complementario con tono cálido para dar calidez -->
    <div
      class="absolute inset-0 bg-linear-to-bl from-tertiary/50 via-black/50 to-secondary/50"
    >
    </div>
  </div>

  <!-- Placeholder para el contenido (vacío por ahora) -->
  <div class="relative z-10 container mx-auto px-4 md:px-6">
    <div class="flex flex-col items-center text-center mb-12">
      <!-- Ícono de Miranti -->
      <img src="/miranti/icon.svg" alt="Miranti" class="h-16 md:h-24 mb-6" />

      <!-- Encabezado con palabras destacadas -->
      <h2
        class="text-4xl md:text-5xl lg:text-6xl font-serif font-bold text-white mb-6 max-w-2xl text-pretty"
      >
        Empieza a
        <span class="text-secondary">vender</span>
        tus productos en solo
        <span class="text-secondary">tres clics</span>
      </h2>

      <!-- Línea decorativa -->
      <div class="w-24 h-1 bg-secondary rounded-full mb-8"></div>
    </div>

    <!-- Descripción introductoria -->
    <div class="max-w-4xl mx-auto mb-16 text-center">
      <p class="text-lg md:text-xl text-white/80">
        Crear tu presencia digital con Miranti es sencillo y rápido. Con nuestra
        plataforma optimizada para dispositivos móviles y conexiones variables,
        podrás mostrar tus productos al mundo en cuestión de minutos, sin
        conocimientos técnicos ni inversiones costosas.
      </p>
    </div>

    <!-- Tarjetas con pasos -->
<div class="grid grid-cols-1 md:grid-cols-3 gap-6 md:gap-8">
    {stepsData.map((step, index) => (
      <div class="bg-white backdrop-blur-md rounded-4xl border border-white/20 shadow-xl ring-1 ring-white/30 overflow-hidden flex flex-col h-full transform transition-transform hover:scale-[1.02]">
        <!-- Contenedor de imagen/icono con padding -->
        <div class="px-4 pt-4 relative">
            <div class="rounded-2xl overflow-hidden">
            <img
              src={step.image}
              alt={step.title}
              class="w-full h-48 object-cover"
            />
          </div>
          <!-- Número superpuesto -->
          <div class="absolute -bottom-5 right-5 bg-secondary rounded-full w-10 h-10 flex items-center justify-center shadow-lg border border-white/30">
            <span class="text-primary font-bold text-lg">{index + 1}</span>
          </div>
        </div>
        
        <!-- Contenido de la tarjeta -->
        <div class="p-6 pt-8 flex-1 flex flex-col">
          <h3 class="text-xl md:text-2xl font-serif font-bold text-primary mb-4">
            {step.title}
          </h3>
          <p class="text-primary/90 flex-1">
            {step.content}
          </p>
        </div>
      </div>
    ))}
  </div>

    <!-- CTA button -->
    <div class="mt-16 text-center">
      <a
        href="/registro"
        class="inline-flex items-center justify-center gap-2 bg-secondary hover:bg-secondary/90 text-primary font-bold py-3 px-8 rounded-full transition-all shadow-lg hover:shadow-xl hover:scale-105 border border-white/30 text-lg"
      >
        Quiero comenzar
        <ArrowRight size={20} stroke-width={2} />
      </a>
    </div>
  </div>
</section>
